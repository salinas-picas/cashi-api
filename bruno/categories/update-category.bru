meta {
  name: Update Category
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/categories/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Alimentación editada"
  }
}
